[build]
builder = "nixpacks"
buildCommand = "pnpm install --no-frozen-lockfile && pnpm --filter backend build"

[deploy]
startCommand = "node --loader tsx/esm src/index.ts"
restartPolicyType = "on_failure"
